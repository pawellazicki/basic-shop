export enum BasicValues {
  APP_NAME = 'Awesome Shop'
}

export enum AnimationState {
    SHOW = 'show',
    HIDE = 'hide'
}

export enum NavigationLabelEnum {
  HOME = 'Home',
  TODOS = 'Todos'
}

export enum NavigationRouteEnum {
  HOME = 'home',
  TODOS = 'todos',
  USER = 'user'
}